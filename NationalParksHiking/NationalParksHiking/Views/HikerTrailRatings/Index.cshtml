@model IEnumerable<NationalParksHiking.Models.HikerTrailRating>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hiker.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trail.TrailName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RatingAmt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hiker.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trail.TrailName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RatingAmt)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HikerTrailRatingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HikerTrailRatingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HikerTrailRatingId })
        </td>
    </tr>
}

</table>
